# Config Source

Small helpers for robot identity, ports, constants, and measured drive config
live here. Most numbers should stay in the matching headers so they are easy to
find during pit fixes.

Typical edits:

- Change the default robot while testing one brain.
- Add a log when a sensor port is still unknown.
- Update comments after a value is measured.
