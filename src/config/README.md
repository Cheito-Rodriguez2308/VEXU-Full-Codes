# Config Source

Implementation for robot identity, ports, constants, and measured robot config.
Students should replace `TODO_PLACEHOLDER` values here after measuring the real
robot and tuning with repeatable tests.
